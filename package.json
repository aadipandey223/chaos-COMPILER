{
  "name": "chaos-compiler-root",
  "version": "1.0.0",
  "description": "Unified Chaos Compiler Project",
  "scripts": {
    "install-all": "npm install && cd server && npm install && cd ../client && npm install",
    "server": "cd server && node index.js",
    "client": "cd client && npm run dev",
    "chaos": "concurrently --kill-others \"npm run server\" \"npm run client\"",
    "build": "cd client && npm install && npm run build",
    "build-compiler": "gcc -O2 compiler/main.c compiler/lexer.c compiler/parser.c compiler/ast.c compiler/chaos.c -o compiler/chaos-compiler -lm"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
